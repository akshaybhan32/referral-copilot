bundle:
  name: referral-copilot

variables:
  postgres_branch:
    description: Full Lakebase Postgres branch resource name. Obtain by running `databricks postgres list-branches {project-name}`, select the desired item from the output array and use its .name value. Requires the project resource name.
  postgres_database:
    description: Full Lakebase Postgres database resource name. Obtain by running `databricks postgres list-databases {branch-name}`, select the desired item from the output array and use its .name value. Requires the branch resource name.
  postgres_project:
    description: Full Lakebase Postgres project resource name. Obtain by running `databricks postgres list-projects`, select the desired item from the output array and use its .name value.

resources:
  apps:
    app:
      name: "referral-copilot"
      description: "Referral Copilot: care-need + location -> evidence-attached facility shortlist"
      source_code_path: ./
      # Uncomment to enable on behalf of user API scopes. Available scopes: sql, dashboards.genie, files.files, serving.serving-endpoints
      # user_api_scopes:
      #   - sql

      # The resources which this app has access to.
      resources:
        - name: postgres
          postgres:
            branch: ${var.postgres_branch}
            database: ${var.postgres_database}
            permission: CAN_CONNECT_AND_CREATE
        - name: embed
          serving_endpoint:
            name: databricks-gte-large-en
            permission: CAN_QUERY
        - name: llm
          serving_endpoint:
            name: databricks-meta-llama-3-3-70b-instruct
            permission: CAN_QUERY

targets:
  default:
    default: true
    workspace:
      host: https://dbc-f9b1513e-43b2.cloud.databricks.com

    variables:
      postgres_branch: projects/referral-copilot/branches/production
      postgres_database: projects/referral-copilot/branches/production/databases/databricks-postgres
      postgres_project: projects/referral-copilot
